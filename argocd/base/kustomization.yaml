apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - harbor.yaml
  - postgres-deploy.yaml
  - app-deploy.yaml
  - secret.yaml
  - updater.yaml
  - ingress.yaml
labels:
  - includeSelectors: true
    pairs:
      app: event-manager
